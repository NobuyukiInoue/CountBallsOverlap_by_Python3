# This is pattern file.

# start_wait = 10      # second.
max_width = 800
max_height = 480
max_balls = 200
radius = 5
movement = -5, 5, 1    # min, max, step
# sleep_sec = 0.01
bar = false
# bar = 200, 200, 10       # pos_x, bar_height, bar_width
heal = 100
ratio_of_balls_stopped = 0.0    # Between 0.0 and 1.0
